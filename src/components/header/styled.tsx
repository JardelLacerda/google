import { styled } from "styled-components";

export const CHeader = styled.header`
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: flex-end;

    button {
        margin: 0 3px 0 3px;
    }
`